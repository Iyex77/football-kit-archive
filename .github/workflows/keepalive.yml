name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 12 */6 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping website
        run: |
          curl -I https://football-kit-archive.vercel.app
